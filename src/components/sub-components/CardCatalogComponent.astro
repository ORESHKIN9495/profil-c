---
const { id, name, image } = Astro.props;
---

<article>
  <img src={image} alt={name} />
  <h5>{name}</h5>
  <a href="/">ГОСТ 10704-91 Скачать (348 Кб)</a>
  <a href=`/catalog/post-${id}` class="button">Перейти к товарам</a>
</article>

<style lang="scss">
  article {
    border: 1px solid #eeeeee;
    display: grid;
    gap: 10px;
    padding: 20px;
    place-items: center;
    text-align: center;
  }
</style>
