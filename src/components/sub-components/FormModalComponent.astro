---

---

<div>
  <form action="#">
    <h3>Сделать заказ</h3>

    <label>
      ФИО
      <input type="text" />
    </label>

    <label>
      Телефон
      <input type="text" />
    </label>

    <label>
      Эл.почта
      <input type="text" />
    </label>

    <label>
      Предполагаемый объем заказа
      <input type="text" />
    </label>

    <label>
      Комментарий
      <textarea name="" id="" cols="30" rows="5"></textarea>
    </label>

    <button class="button">Отправить заказ</button>
  </form>
</div>

<style lang="scss">
  div {
    background-color: rgba(82, 162, 219, 0.6);
    backdrop-filter: blur(5px);
    position: fixed;
    inset: 0;
    display: grid;
    place-items: center;
    z-index: 9;

    form {
      background-color: var(--c-inverted);
      color: var(--c-theme);
      display: grid;
      grid-template: auto / repeat(2, 1fr);
      gap: 20px;
      margin: auto;
      padding: clamp(20px, 7vw, 100px);

      label {
        display: grid;

        &:first-of-type {
          grid-column: 1 / -1;
        }

        &:nth-of-type(4) {
          grid-column: 1 / -1;
        }

        &:last-of-type {
          grid-column: 1 / -1;
        }

        input,
        textarea {
          border: 1px solid var(--c-secondary);
          margin: 10px 0 0;
          padding: 10px;
        }

        button {
          grid-column: 1 / -1;
        }
      }
    }
  }
</style>
